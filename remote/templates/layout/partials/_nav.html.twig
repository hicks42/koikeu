			<ul class="menu">
				{% if app.user %} 
					<li class="menu-inline-item">
						<a class="menu-link" href=" {{ path('app_account')}} ">{{ app.user.FullName }}</a>
					</li> 
					<li class="menu-inline-item">
						<a class="menu-link" href="{{path('app_produits_create')}}"><i class="fas fa-box-open"></i></a>
					</li>
					<li class="menu-inline-item">
						<a class="menu-link" href="#" onclick="event.preventDefault(); document.getElementById('js-logout-form').submit();" ><i class="fas fa-sign-out-alt"></i></a>
					</li>
					<form id="js-logout-form" action=" {{path('app_logout')}} " method="POST" style="display: none;">
						<input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
					</form>
				{% else %}
					<li class="menu-inline-item">
						<a class="menu-link" href="{{path('app_login')}}"><i class="fas fa-leaf"></i></a>
					</li>
				{% endif %}
				{% if is_granted('ROLE_ADMIN') %}
					<li class="menu-inline-item">
						<a class="menu-link" href=" {{ path('app_user')}} "><i class="fas fa-users-cog"></i></a>
					</li>
					<li class="menu-inline-item">
						<a class="menu-link" href="{{path('app_register')}}"><i class="fas fa-user-plus"></i><a>
					</li>
				{% endif %}
			</ul>