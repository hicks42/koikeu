{% extends 'base.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-md-6 mx-auto">
			<h1 class="text-center text-muted">Edit User</h1>
			<a class="nav-link" href="#" onclick="event.preventDefault(); document.getElementById('js-logout-form').submit();" >Logout</a>
			{{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

			<a href="{{ path('app_user') }}" class="btn btn btn-outline-secondary m-2">Back</a>

			{{ include('user/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
