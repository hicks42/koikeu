{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>
<article class="column">
    <table class="table">
        <tbody>
            <tr>
                <th>Id : {{ category.id }}</th>
                <td><img src={{ (category.imageName ? vich_uploader_asset(category) : asset('build/images/placeholder.jpg')) | imagine_filter('square_thumbnail_small') }}></td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}" class="btn btn btn-outline-secondary m-2">Retour à la liste</a>

    <a href="{{ path('category_edit', {'id': category.id}) }}" class="btn btn btn-outline-secondary m-2">Modifier</a>

    {{ include('category/_delete_form.html.twig') }}
</article>
{% endblock %}
